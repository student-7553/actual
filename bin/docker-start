#!/bin/sh

#####################################################
# This startup script is used by the docker container
# to install dependencies (or refresh a stale host
# node_modules mount) and start the browser app.
#####################################################

yarn install

BROWSER=0 yarn start:browser
